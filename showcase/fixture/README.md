# 公开演示数据

`course.json` 是 `exam-kit` 的 **derived demo**：内容为独立措辞的 MySQL 学习示例，不代表任何真实课程、教师或学校。

它只演示已经准备好的知识骨架和题库如何通过仓库中的三个 renderer 生成离线 HTML。它不包含真实课件、原始图片、连续课件原文、私人信息或本地路径，也不调用模型和外部网络。

真实用户流程仍由 Agent 根据用户提供的课程资料完成扫描、提取、知识骨架整理和命题；公开 showcase 只为确定性地复现最后的渲染链路。